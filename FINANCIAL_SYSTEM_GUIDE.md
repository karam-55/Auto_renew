# دليل النظام المالي - Auto Renew
## كيف يعمل المحاسبة والتقارير المالية

---

## 1. نظرة عامة على النظام المالي

النظام المالي في Auto Renew يعمل على **مبدأ القيود المزدوجة (Double Entry Accounting)** — كل عملية مالية تُسجل في جانبين: **مدين (Debit)** و**دائن (Credit)**.

### المكونات الرئيسية:
- **شجرة الحسابات (Chart of Accounts)** — دليل الحسابات الهرمجي
- **القيود اليومية (Journal Entries)** — السجلات المالية
- **دفتر الأستاذ (General Ledger)** — الحسابات التفصيلية
- **الفواتير والمدفوعات** — العمليات التلقائية
- **التقارير المالية** — قائمة الدخل، الميزانية العمومية، ميزان المراجعة

---

## 2. شجرة الحسابات — كيف تنشأ؟

### عند أول تشغيل للنظام:

```
الخطوة 1: Setup Wizard ← بتشغله من شاشة الإعداد
الخطوة 2: Step 3 ← "Chart of Accounts" ← اضغط "Create Default Accounts"
```

### هيكل الحسابات الافتراضي:

```
1xxx — الأصول (Assets)
├─ 1100 — الأصول المتداولة
│  ├─ 1110 — النقدية بالصندوق
│  ├─ 1120 — النقدية بالبنوك
│  ├─ 1130 — العملاء (الذمم المدينة)
│  ├─ 1140 — المخزون
│  └─ 1150 — مصروفات مدفوعة مقدماً
└─ 1200 — الأصول الثابتة
   ├─ 1210 — الأراضي
   ├─ 1220 — المباني
   ├─ 1230 — المعدات
   ├─ 1240 — المركبات
   └─ 1260 — مجمع الاستهلاك (عكسي)

2xxx — الخصوم (Liabilities)
├─ 2100 — الخصوم المتداولة
│  ├─ 2110 — الموردون (الذمم الدائنة)
│  ├─ 2120 — قروض قصيرة
│  ├─ 2130 — ضريبة القيمة المضافة
│  ├─ 2140 — رواتب مستحقة
│  └─ 2160 — أقساط مستحقة
└─ 2200 — الخصوم طويلة الأجل

3xxx — حقوق الملكية (Equity)
├─ 3100 — رأس المال
├─ 3200 — الأرباح المحتجزة
│  └─ 3210 — أرباح العام الحالي
└─ 3400 — خسائر العام الحالي

4xxx — الإيرادات (Revenue)
├─ 4100 — إيرادات الخدمات
│  ├─ 4110 — صيانة المركبات
│  ├─ 4120 — قطع الغيار
│  ├─ 4130 — الغسيل والتلميع
│  └─ 4140 — الفحص الدوري
└─ 4200 — إيرادات أخرى

5xxx — تكاليف البضاعة (COGS)
├─ 5100 — تكلفة قطع الغيار
├─ 5200 — تكلفة المواد المستهلكة
└─ 5300 — تكلفة العمالة المباشرة

6xxx — المصروفات التشغيلية
├─ 6100 — رواتب وأجور
├─ 6200 — إيجار
├─ 6300 — كهرباء وماء
├─ 6400 — إهلاك
├─ 6500 — صيانة
├─ 6600 — وقود ونقل
├─ 6700 — تسويق
└─ 6800 — إدارة ومكتب

7xxx — المصروفات المالية
├─ 7100 — فوائد بنكية
├─ 7200 — عمولات بنكية
└─ 7300 — خسائر صرف العملة
```

### كيف تُنشئ الحسابات؟

**تلقائياً:** من Setup Wizard ← Step 3 ← "Create Default Accounts"

**يدوياً:** من شاشة `شجرة الحسابات` ← زر "إضافة حساب"
- الرمز (Code): مثلاً `6101` لحساب فرعي تحت الرواتب
- الاسم (Ar/En)
- النوع: ASSET / LIABILITY / EQUITY / REVENUE / EXPENSE / COGS
- الحساب الأب (اختياري): لبناء الهرم

---

## 3. القيود المحاسبية التلقائية — كيف تُنشأ؟

### المبدأ:
كل عملية تجارية تلقائياً تُنشئ **قيد محاسبي** — ما عليك إلا تعمل الفاتورة أو الدفع، والنظام يسجل القيد لوحده.

### 3.1. عند إصدار فاتورة (Invoice Issued)

**ما يحدث تلقائياً:**
```
مدين: 1130 — العملاء (الذمم المدينة) ← بمبلغ إجمالي الفاتورة
دائن: 4110 — إيرادات صيانة المركبات ← بمبلغ الخدمات
دائن: 4120 — إيرادات قطع الغيار ← بمبلغ القطع
دائن: 2130 — ضريبة القيمة المضافة ← بمبلغ الضريبة
```

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:334`

### 3.2. عند استلام دفعة (Payment Received)

**ما يحدث تلقائياً:**
```
مدين: 1110 — النقدية بالصندوق (أو 1120 — البنك)
دائن: 1130 — العملاء (الذمم المدينة) ← بنفس المبلغ
```

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:438`

### 3.3. عند استلام مخزون (GRN — Goods Receipt)

**ما يحدث تلقائياً:**
```
مدين: 1140 — المخزون ← بتكلفة البضاعة
دائن: 2110 — الموردون (الذمم الدائنة)
```

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:525`

### 3.4. عند استهلاك مخزون (Stock Consumption)

**ما يحدث تلقائياً:**
```
مدين: 5100 — تكلفة قطع الغيار المباعة
دائن: 1140 — المخزون
```

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:589`

### 3.5. عند دفع راتب (Payroll)

**ما يحدث تلقائياً:**
```
مدين: 6100 — مصروفات الرواتب والأجور ← بصافي الراتب
دائن: 1110 — النقدية بالصندوق ← بصافي الراتب
```

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:970`

---

## 4. كيف تُحدّث أرصدة الحسابات؟

### قاعدة التحديث:

| نوع الحساب | المدين يزيد | الدائن يزيد |
|------------|-------------|-------------|
| **أصول (Assets)** | نعم | لا |
| **خصوم (Liabilities)** | لا | نعم |
| **حقوق ملكية (Equity)** | لا | نعم |
| **إيرادات (Revenue)** | لا | نعم |
| **مصروفات (Expenses)** | نعم | لا |
| **تكاليف (COGS)** | نعم | لا |

**مثال عملي:**
- إذا العملاء (1130) مدين 100,000 → الرصيد يزيد 100,000
- إذا العملاء (1130) دائن 50,000 → الرصيد ينقص 50,000
- إذا إيرادات الخدمات (4110) دائن 80,000 → الرصيد يزيد 80,000

**في الكود:** `backend/src/modules/accounting/automatic-journal-entries.ts:280`

---

## 5. كيف تجرب النظام المالي عملياً؟

### المتطلبات المسبقة:
1. **تشغيل Backend** على `http://localhost:8080`
2. **تشغيل Frontend** (flutter_admin أو admin_tauri)
3. **إكمال Setup Wizard** على الأقل لـ Step 3 (Chart of Accounts)
4. **إنشاء فترة مالية مفتوحة** (Fiscal Period)

---

### السيناريو التجريبي الكامل:

#### الخطوة 1: إعداد البيئة
```bash
# 1. شغّل Backend
cd backend
npm run dev

# 2. شغّل Frontend (Flutter)
cd flutter_admin
flutter run -d windows

# 3. سجّل الدخول:
# Username: owner
# Password: owner123
```

#### الخطوة 2: أكمل Setup Wizard (إذا لم تكمل من قبل)

```
الشاشة: /setup-wizard
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
الخطوة 1: بيانات الشركة
  ← املأ اسم الشرمة، العنوان، الرقم الضريبي

الخطوة 2: الإعدادات المالية
  ← سعر الصرف، نسبة الضريبة، الفترة المالية
  مثال: fiscalPeriodName: "2026"
         fiscalStartDate: "2026-01-01"
         fiscalEndDate: "2026-12-31"

الخطوة 3: شجرة الحسابات ⭐ مهم جداً
  ← ضع صح على "Create Default Accounts"
  ← اضغط "حفظ"
  ← النظام ينشئ 60+ حساب تلقائياً

الخطوة 4-6: اختيارية ← اضغط "تخطي" أو أكمل
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### الخطوة 3: تحقق من شجرة الحسابات

```
انتقل إلى: الحسابات ← شجرة الحسابات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ما تتوقعه:
├─ ترى قائمة بالحسابات (1110، 1120، 1130، 1140...)
├─ كل حساب يظهر: الرمز، الاسم، النوع، الرصيد
├─ الأرصدة تبدأ بـ 0
└─ إذا ضغطت "إضافة حساب" ← تقدر تضيف حساب فرعي
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### الخطوة 4: أنشئ عميل + مركبة + حجز

```
الشاشة: /customers
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. زر "عميل جديد"
   ← الاسم: "أحمد محمد"
   ← الموبايل: "0912345678"
   ← العنوان: "دمشق"
   ← الحالة: نشط
   ← احفظ

2. انتقل إلى المركبات ← أضف مركبة للعميل
   ← النوع: سيارة
   ← الماركة: Toyota
   ← الموديل: Corolla
   ← اللوحة: "12345 س"

3. انتقل إلى الحجوزات ← "حجز جديد"
   ← اختر العميل: أحمد محمد
   ← اختر المركبة
   ← الخدمة: "تغيير زيت"
   ← تاريخ: اليوم
   ← الحالة: مؤكد
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### الخطوة 5: أنشئ فاتورة

```
الشاشة: /invoices
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. زر "فاتورة جديدة" ← اختر "فاتورة من حجز"
2. اختر حجز "أحمد محمد — تغيير زيت"
3. أضف بنود:
   ├─ الخدمة: "تغيير زيت" ← السعر: 50,000 ل.س
   ├─ القطعة: "زيت محرك" ← الكمية: 4 ← السعر: 15,000 ل.س
   └─ الضريبة: 11% ← تحسب تلقائياً
4. اضغط "إصدار الفاتورة"

النتيجة المتوقعة:
├─ الفاتورة تنتقل من "مسودة" إلى "مصدرة"
├─ القيد المحاسبي يُنشأ تلقائياً
└─ تذهب إلى "القيود اليومية" ← تلاقي قيد جديد
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### الخطوة 6: تحقق من القيد المحاسبي التلقائي

```
الشاشة: /accounting ← القيود اليومية
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ما تتوقعه:
├─ ترى قيد بتاريخ اليوم
├─ المرجع: رقم الفاتورة
├─ البيان: "Invoice #001 - أحمد محمد"
├─ مدين: 113,650 (العملاء)
├─ دائن: 100,000 (إيرادات الخدمات) + 13,650 (ضريبة)
└─ الحالة: مرحل (POSTED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**القيد المتوقع:**
```
مدين: 1130 — العملاء (الذمم المدينة)        113,650 ل.س
دائن: 4110 — إيرادات صيانة المركبات          50,000 ل.س
دائن: 4120 — إيرادات قطع الغيار              60,000 ل.س
دائن: 2130 — ضريبة القيمة المضافة            13,650 ل.س
                                              ————
المجموع:                                       0 ✅
```

#### الخطوة 7: سجّل دفعة

```
الشاشة: /invoices ← اختر الفاتورة ← زر "دفع"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. طريقة الدفع: نقدي
2. المبلغ: 113,650 ل.س
3. اضغط "تسجيل الدفع"

النتيجة:
├─ حالة الفاتورة تتغير إلى "مدفوعة"
├─ قيد دفع يُنشأ تلقائياً
└─ رصيد "العملاء" ينقص، و"الصندوق" يزيد
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**القيد المتوقع:**
```
مدين: 1110 — النقدية بالصندوق              113,650 ل.س
دائن: 1130 — العملاء (الذمم المدينة)        113,650 ل.س
                                              ————
المجموع:                                       0 ✅
```

#### الخطوة 8: تحقق من الأرصدة في شجرة الحسابات

```
الشاشة: /accounting ← شجرة الحسابات
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ما تتوقعه الآن:
├─ 1110 — النقدية بالصندوق:       +113,650 ✅
├─ 1130 — العملاء:                      0 ✅ (المدين = الدائن)
├─ 2130 — ضريبة القيمة المضافة:    +13,650 ✅
├─ 4110 — إيرادات صيانة:          +50,000 ✅
└─ 4120 — إيرادات قطع الغيار:     +60,000 ✅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

#### الخطوة 9: جرّب التقارير المالية

```
الشاشة: /reports
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. قائمة الدخل (/accounting/income-statement)
   ├─ الإيرادات: 110,000 ل.س
   ├─ تكلفة البضاعة: 0 (لم نستهلك مخزون)
   ├─ إجمالي الربح: 110,000 ل.س
   ├─ المصروفات: 0
   └─ صافي الربح: 110,000 ل.س

2. الميزانية العمومية (/accounting/balance-sheet)
   ├─ إجمالي الأصول: 113,650 (الصندوق)
   ├─ إجمالي الخصوم: 13,650 (الضريبة)
   └─ حقوق الملكية: 100,000 (الأرباح)
   ✅ المعادلة: الأصول = الخصوم + حقوق الملكية

3. ميزان المراجعة (/accounting/trial-balance)
   ├─ إجمالي المدين = إجمالي الدائن
   └─ الحالة: متوازن ✅

4. دفتر الأستاذ (/accounting/general-ledger)
   ├─ يظهر كل الحركات التفصيلية
   ├─ تاريخ، حساب، مدين، دائن، رصيد
   └─ يمكن البحث بالحساب
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 6. التقارير المالية المتاحة

### التقارير الأساسية:

| التقرير | المسار | ما يعرض |
|---------|--------|---------|
| **قائمة الدخل** | `/accounting/income-statement` | الإيرادات، التكاليف، المصروفات، صافي الربح |
| **الميزانية العمومية** | `/accounting/balance-sheet` | الأصول، الخصوم، حقوق الملكية |
| **ميزان المراجعة** | `/accounting/trial-balance` | إجمالي مدين/دائن لكل حساب |
| **دفتر الأستاذ** | `/accounting/general-ledger` | الحركات التفصيلية حسب الحساب |
| **القيود اليومية** | `/accounting/journal-entries` | جميع القيود المحاسبية |
| **شجرة الحسابات** | `/accounting/chart-of-accounts` | دليل الحسابات الهرمجي |

### التقارير المتقدمة:

| التقرير | المسار | ما يعرض |
|---------|--------|---------|
| **كشف حساب عميل** | `/accounting/customer-statement` | حركات عميل معين |
| **كشف حساب مورد** | `/accounting/supplier-statement` | حركات مورد معين |
| **الذمم المدينة** | `/reports/aging-reports` | فواتير غير مدفوعة حسب التأخير |
| **تحليل التكاليف** | `/reports/cost-analysis` | تكاليف الخدمات والقطع |
| **هوامش الربح** | `/reports/margin-analysis` | الربح حسب الخدمة/العميل |
| **اتجاه الإيرادات** | `/reports/revenue-trend` | إيرادات شهرية مع توقعات |
| **ربحية الخدمات** | `/reports/service-profitability` | ربح كل خدمة على حدة |
| **التدفق النقدي** | `/accounting/cash-flow` | حركة النقدية |

---

## 7. الأدوار والصلاحيات

| الدور | الصلاحيات المالية |
|-------|-----------------|
| **OWNER** | كل شيء |
| **MANAGER** | عرض التقارير + الموافقة على المصروفات |
| **ACCOUNTANT** | القيود اليومية + التقارير + شجرة الحسابات |
| **CASHIER** | المدفوعات + عرض الفواتير |
| **RECEPTIONIST** | إنشاء الفواتير + عرض تقارير بسيطة |
| **MECHANIC** | لا يوجد |

---

## 8. نصائح مهمة

### 1. قبل إنشاء أي فاتورة:
```
✅ تأكد من إكمال Setup Wizard Step 3 (Chart of Accounts)
✅ تأكد من وجود فترة مالية مفتوحة (Fiscal Period)
✅ تأكد من تشغيل Backend (8080)
```

### 2. إذا لم تظهر التقارير بيانات:
```
❌ المشكلة: لم تُصدر أي فاتورة بعد
✅ الحل: أنشئ فاتورة وادفعها ← البيانات تظهر تلقائياً

❌ المشكلة: القيود لا تظهر
✅ الحل: تحقق من إكمال Setup Wizard ← Step 3

❌ المشكلة: أرصدة الحسابات = 0
✅ الحل: سجّل دفعة ← الأرصدة تتحدث فوراً
```

### 3. فهم مبدأ المدين والدائن:
```
┌─────────────────────────────────────────────────────┐
│  الأصول ↑ بالمدين    │    الأصول ↓ بالدائن        │
│  الخصوم ↑ بالدائن    │    الخصوم ↓ بالمدين        │
│  الإيرادات ↑ بالدائن  │    المصروفات ↑ بالمدين    │
└─────────────────────────────────────────────────────┘
```

### 4. تسلسل العمليات الصحيح:
```
1. إعداد النظام (Setup Wizard)
2. إضافة العملاء والمركبات
3. إنشاء الحجوزات
4. إصدار الفواتير ← القيود تنشأ تلقائياً
5. تسجيل المدفوعات ← الأرصدة تتحدث
6. مراجعة التقارير ← البيانات تتفاعل
```

---

## 9. API Endpoints للتجريب السريع

### عبر Postman أو cURL:

```bash
# 1. تسجيل الدخول
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"owner","password":"owner123"}'

# 2. الحصول على التوكن ← استخدمه في Header: Authorization: Bearer <token>

# 3. عرض شجرة الحسابات
curl http://localhost:8080/api/accounts \
  -H "Authorization: Bearer <token>"

# 4. عرض القيود اليومية
curl http://localhost:8080/api/journal-entries \
  -H "Authorization: Bearer <token>"

# 5. عرض قائمة الدخل
curl http://localhost:8080/api/reports/profit-loss \
  -H "Authorization: Bearer <token>"

# 6. عرض الميزانية العمومية
curl http://localhost:8080/api/reports/balance-sheet \
  -H "Authorization: Bearer <token>"

# 7. عرض ميزان المراجعة
curl http://localhost:8080/api/reports/trial-balance \
  -H "Authorization: Bearer <token>"
```

---

## 10. ملخص سريع — ابدأ هنا

| أنا أريد... | اذهب إلى... | اضغط... |
|-------------|-------------|---------|
| إعداد النظام للمرة الأولى | `/setup-wizard` | أكمل الخطوات 1-3 |
| التأكد من الحسابات | `/accounting/chart-of-accounts` | تحقق من وجود 1110، 1130، 4110 |
| إنشاء فاتورة | `/invoices` | "فاتورة جديدة" |
| تسجيل دفعة | `/invoices` ← اختر فاتورة | زر "دفع" |
| رؤية القيود | `/accounting/journal-entries` | تحقق من القيد التلقائي |
| رؤية الأرباح | `/accounting/income-statement` | شاهد صافي الربح |
| رؤية الأرصدة | `/accounting/balance-sheet` | تحقق من المعادلة |
| رؤية الحركات التفصيلية | `/accounting/general-ledger` | ابحث بالحساب |

---

**تاريخ الإنشاء:** 2026-06-20  
**الإصدار:** 1.0  
**الملف:** `FINANCIAL_SYSTEM_GUIDE.md`
